const kelvin=0;//constant kelvin defined
const celsius=kelvin-273;//conversion to celsius
let fahrenheit=celsius*(9/5)+32;//conversion to fahrenheit
fahrenheit=Math.floor(fahrenheit);//rounding off the value
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);

var kelvin=0;//constant kelvin defined
var celsius=kelvin-273;//conversion to celsius
let Newton=celsius*(33/100);//conversion to Newton
Newton=Math.floor(Newton);//rounding off the value
console.log(`The temperature is ${Newton} degrees Fahrenheit.`);
